# Splash Screen

Place your splash screen icon here:

- `splash_icon.png` - Splash screen icon (512x512 px recommended)

After adding splash icon, run:
```bash
flutter pub run flutter_native_splash:create
```

The splash screen background color is set to #0f172a (dark blue-gray) in pubspec.yaml.

